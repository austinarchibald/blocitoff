<h5>Add a new item:</h5>

<div class="form-group">
  <%= form_for Item.new do |f| %>
    <%= f.text_field :name, :class => 'form-control', placeholder: 'To-do item' %>
</div>

<div class="form-group">
  <%= f.submit "Add Item", :class => 'btn btn-primary' %>
</div>
<% end %>
